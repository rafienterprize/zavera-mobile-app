# 🚨 REMINDER: PUSH KE BRANCH RAFFI

## ⚠️ PENTING! Setiap kali ada perubahan:

### 1. Cek Status
```bash
git status
```

### 2. Add Perubahan
```bash
git add .
```

### 3. Commit dengan Pesan Jelas
```bash
git commit -m "feat: deskripsi perubahan"
```

### 4. Push ke Branch Raffi
```bash
git push origin raffi
```

## 📝 Format Commit Message:

- `feat:` → Fitur baru
- `fix:` → Fix bug
- `refactor:` → Refactor code
- `docs:` → Update dokumentasi
- `style:` → Format code

## ✅ Contoh:

```bash
git add .
git commit -m "feat: add login screen UI"
git push origin raffi
```

## 🔔 Kapan Harus Push?

- ✅ Setelah selesai fitur kecil
- ✅ Sebelum istirahat/pulang
- ✅ Sebelum pindah ke fitur lain
- ✅ Minimal 1x sehari

## ⚡ Quick Command:

```bash
git add . && git commit -m "feat: update" && git push origin raffi
```

---

**JANGAN LUPA PUSH! 🚀**
